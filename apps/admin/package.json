{
  "name": "@eo-ms/admin",
  "version": "0.0.0",
  "private": true,
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "clean": "run-p clean:**",
    "clean:cache": "rimraf .turbo && rimraf dist",
    "clean:deps": "rimraf node_modules"
  },
  "engines": {
    "node": ">=16"
  }
}
